server.port=8080
spring.application.name=sheetalchain-backend

# LocalStack DynamoDB config (we'll use this in our AWS config class next)
aws.region=us-east-1
aws.dynamodb.endpoint=http://localhost:4566